import { SearchRegistration } from './search-registration';

describe('SearchRegistration', () => {
  it('should create an instance', () => {
    expect(new SearchRegistration()).toBeTruthy();
  });
});